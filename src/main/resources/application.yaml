spring:
  application:
    name: discodeit
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
      enabled: true

discodeit:
  repository:
    type: file   # jcf | file
    file-directory: .discodeit