spring:
  application:
    name: discodeit
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 30MB

server:
  port: ${PORT:8080}

discodeit:
  repository:
    type: file
    file-directory: .discodeit