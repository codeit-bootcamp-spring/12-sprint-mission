spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: discodeit
  profiles:
    active: dev
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 30MB
  jpa:
    open-in-view: false
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: discodeit-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  cache:
    # caffeine: 인스턴스마다 각자 들고 있는 로컬 캐시
    # redis   : 여러 인스턴스가 공유하는 전역 캐시
    type: ${CACHE_TYPE:caffeine}
    cache-names:
      - channels
      - notifications
      - users
    caffeine:
      # recordStats: /actuator/metrics/cache.* 에서 히트/미스를 볼 수 있게 한다
      spec: >
        maximumSize=100,
        expireAfterAccess=600s,
        recordStats
    redis:
      enable-statistics: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}


management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers,caches
  info:
    env:
      enabled: true
  # @Timed, @Counted 같은 관측 어노테이션을 활성화한다 (기본값 false)
  observations:
    annotations:
      enabled: true

info:
  app:
    name: Discodeit
    version: 2.2-M11
    java-version: "17"
    spring-boot-version: "3.4.0"

discodeit:
  event:
    # spring: 같은 프로세스의 @TransactionalEventListener가 알림을 생성한다
    # kafka : 이벤트를 Kafka로 내보내고, 별도 서비스로 가정한 @KafkaListener가 알림을 생성한다
    transport: ${EVENT_TRANSPORT:spring}
  # 아래 값들은 기본값을 두지 않는다.
  # fallback이 있으면 환경변수 주입을 빠뜨려도 예측 가능한 자격증명으로 조용히 기동해버리므로,
  # 주입 누락 시 곧바로 기동에 실패하도록 한다(fail-fast). 로컬 개발값은 dev 프로파일에 둔다.
  admin:
    username: ${ADMIN_USERNAME}
    email: ${ADMIN_EMAIL}
    password: ${ADMIN_PASSWORD}
  security:
    jwt:
      # HS256 서명 키. 32바이트 미만이면 기동에 실패한다.
      # 키가 저장소에 있으면 누구나 토큰을 위조할 수 있으므로 기본값을 두지 않는다.
      secret: ${JWT_SECRET}
      access-token-validity-seconds: ${JWT_ACCESS_TOKEN_VALIDITY_SECONDS:1800}          # 30분
      refresh-token-validity-seconds: ${JWT_REFRESH_TOKEN_VALIDITY_SECONDS:1209600}     # 14일
      # 계정당 동시 로그인 허용 수. 초과하면 가장 오래된 로그인을 무효화한다
      max-active-count: ${JWT_MAX_ACTIVE_COUNT:1}
      # 로테이션 직후 직전 리프레시 토큰을 인정하는 유예 창.
      # 탭 여러 개가 동시에 재발급을 요청하는 정상 상황과 탈취 후 재사용을 구분하기 위한 값으로,
      # 길수록 탈취된 토큰이 살아있는 시간이 늘어나므로 짧게 둔다.
      refresh-grace-seconds: ${JWT_REFRESH_GRACE_SECONDS:10}
      # 운영(HTTPS)에서는 true로 켠다
      cookie-secure: ${JWT_COOKIE_SECURE:false}
  storage:
    type: ${STORAGE_TYPE:local}                              # local | s3 (기본 local)
    local:
      root-path: ${STORAGE_LOCAL_ROOT_PATH:.discodeit/storage}
    s3:
      access-key: ${AWS_S3_ACCESS_KEY}
      secret-key: ${AWS_S3_SECRET_KEY}
      region: ${AWS_S3_REGION}
      bucket: ${AWS_S3_BUCKET}
      presigned-url-expiration: ${AWS_S3_PRESIGNED_URL_EXPIRATION:600}   # 초 단위, 기본 10분

logging:
  level:
    root: info
